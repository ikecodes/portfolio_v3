import React from "react";
import Container from "../layouts/Container";
import Header from "../shared/Header";

const Work = () => {
  return (
    <Container>
      <Header>work</Header>
    </Container>
  );
};

export default Work;
